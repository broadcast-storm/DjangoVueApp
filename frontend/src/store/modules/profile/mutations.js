const mutations = {
    SET_USER_DATA: (state, { user }) => {
        state.userData = user
    } ,
    /* SET_USER_TASKS: (state, { tasks }) => {
        state.tasks = tasks
    }, */
}

export default mutations
