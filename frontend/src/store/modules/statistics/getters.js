const getters = {
    getStatistics: state => state.statistics,
    getReceivedAchieves: state => state.receivedAchieves,
    getIndicators: state => state.indicators,
    getIsLoading: state => state.isLoading,
}

export default getters
